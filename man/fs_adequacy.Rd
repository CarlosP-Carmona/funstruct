% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_adequacy.R
\name{fs_adequacy}
\alias{fs_adequacy}
\title{Sample-size adequacy for density estimation}
\usage{
fs_adequacy(space = NULL, n = NULL, d = NULL)
}
\arguments{
\item{space}{Optional \code{fspace}; if supplied, \code{d} defaults to its current
number of dimensions.}

\item{n}{Observations available per unit (a single value or a vector,
e.g. individuals per species).}

\item{d}{Dimensionality to assess (1 to 10).}
}
\value{
A list of class \code{fs_adequacy} with elements \code{d}, \code{n_required},
\code{n}, \code{adequate} (logical, per element of \code{n}), and \code{table} (the full
requirement table). \code{print()} gives a plain-language verdict.
}
\description{
The number of observations needed for kernel density estimation grows
exponentially with dimensionality: roughly 4 observations suffice in one
dimension, ~220 are needed in four, and ~2800 in six (Silverman 1986).
This diagnostic compares the available observations per unit with those
requirements, to inform the choice of dimensionality \emph{before} estimating
TPDs. Point-based methods degrade over the same gradient, only silently
(convex hulls degenerate and pairwise distances concentrate), so an
inadequate \code{n} is a problem for every engine, not only the probabilistic
one.
}
\examples{
fs_adequacy(n = 50, d = 2)   # feasible
fs_adequacy(n = 50, d = 4)   # not feasible

# per-species sample sizes from the grassland data
data(grassland)
fs_adequacy(n = table(grassland$species), d = 2)
}
\references{
Silverman, B.W. (1986) \emph{Density Estimation for Statistics
and Data Analysis}. Chapman & Hall. (Table 4.2.)
}
\seealso{
\code{\link[=fs_dimensionality]{fs_dimensionality()}}, \code{\link[=fs_quality]{fs_quality()}}
}
