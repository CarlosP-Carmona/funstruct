% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_angles.R
\name{fs_angles}
\alias{fs_angles}
\title{Angles between traits in the retained space}
\usage{
fs_angles(space, dims = NULL, degrees = TRUE, center = FALSE, profile = FALSE)
}
\arguments{
\item{space}{An \code{fspace} with loadings: a PCA space, or a PCoA/NMDS
space after \code{\link[=fs_loadings]{fs_loadings()}}. Typically reduced with \code{\link[=fs_reduce]{fs_reduce()}}.}

\item{dims}{Axes to use (integer indices); default all axes present
in the loadings.}

\item{degrees}{Logical; return degrees (default) or radians.}

\item{center}{Logical; \code{FALSE} (default) for the direct vector angle,
\code{TRUE} for the angle of the centered (Pearson) correlation between
loading vectors.}

\item{profile}{Logical; if \code{TRUE}, also compute the angle matrices at
every dimensionality from 2 to the full space and their agreement
with the full-space angles (see Details). Cannot be combined with
\code{dims}.}
}
\value{
A list of class \code{fs_angles} with \code{angles} (traits x traits
symmetric matrix), \code{cosines}, \code{dims}, \code{degrees} and \code{center}. With
\code{profile = TRUE}, also \code{by_dims} (list of angle matrices, one per
dimensionality) and \code{profile} (data.frame with \code{dims}, \code{cor},
\code{mad}). \code{print()} and \code{plot()} methods are provided
(\code{plot(x, which = "profile")} draws the preservation curve).
}
\description{
Computes the pairwise angles that traits form in the retained
dimensions of a space, from their loading vectors across the axes.
Bivariate trait plots show relationships two traits at a time; the
angles summarize how the whole trait battery is organized in the
multidimensional space (Bueno et al. 2023; Beccari & Carmona 2024):
0 degrees means two traits point in the same direction (perfectly
positively associated as represented in the space), 90 degrees means
they are orthogonal (unrelated), and 180 degrees means they are
opposed.
}
\details{
The trait vectors are the rows of the space's loadings over the
retained axes. For a PCA space these are the construction loadings
(eigenvectors scaled by the component standard deviations); for PCoA
and NMDS spaces run \code{\link[=fs_loadings]{fs_loadings()}} first, and the post-hoc
correlation loadings are used (numeric traits only). In the \emph{full}
PCA space the angle between two traits is exactly
\code{acos(cor(trait_i, trait_j))} -- the classical geometric reading of a
correlation; in a \emph{reduced} space the angles describe the trait
relationships as the retained axes represent them, which is the
interesting object: run \code{\link[=fs_reduce]{fs_reduce()}} first.

By default the angle is the direct vector angle,
\verb{acos(sum(l_i * l_j) / (|l_i| |l_j|))} (the uncentered cosine, which
gives the exact correspondence with trait correlations above). Set
\code{center = TRUE} to use the Pearson correlation between the two
loading vectors instead (centering them across axes first), as done
in some published implementations.

With \code{profile = TRUE} (on the full space, before reducing) the
angles double as a dimensionality diagnostic: the angle matrix is
recomputed at every dimensionality from 2 up to the full space, and
each is compared with the full-space angles -- which are exactly the
raw trait correlations -- through their correlation over trait pairs
and the mean absolute angular deviation (mad, in the units of the
angles). A high correlation at \code{k} dimensions means the reduced
space represents the correlation structure among the original traits
faithfully. This is the trait-side complement of \code{\link[=fs_quality]{fs_quality()}},
which asks the same question about the distances among \emph{units}; the
two need not agree, and a good reduced space should pass both. Note
the correlation runs over the \code{p (p - 1) / 2} trait pairs, so it is
a coarse statistic for small trait batteries -- read it together
with mad.
}
\examples{
data(gspff)
sp2 <- fs_reduce(fs_space(gspff, method = "pca"), 2)
an <- fs_angles(sp2)
an
plot(an)

# angle preservation across dimensionalities (run on the FULL space):
ap <- fs_angles(fs_space(gspff, method = "pca"), profile = TRUE)
ap$profile
plot(ap, which = "profile")

# PCoA route: post-hoc loadings first
spo <- fs_loadings(fs_space(fs_dist(gspff[1:150, ]), "pcoa"),
                   gspff[1:150, ])
fs_angles(fs_reduce(spo, 2))
}
\references{
Bueno, C.G., Toussaint, A., Träger, S., et al. (2023)
Reply to: The importance of trait selection in ecology. \emph{Nature},
618, E31-E34. \doi{10.1038/s41586-023-06149-7}

Beccari, E. & Carmona, C.P. (2024) Aboveground and belowground
sizes are aligned in the unified spectrum of plant form and
function. \emph{Nature Communications}, 15.
\doi{10.1038/s41467-024-53180-x}
}
\seealso{
\code{\link[=fs_loadings]{fs_loadings()}}, \code{\link[=fs_reduce]{fs_reduce()}}, \code{\link[=fs_trait_dim]{fs_trait_dim()}}
}
