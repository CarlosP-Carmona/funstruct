% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_compare.R
\name{fs_compare}
\alias{fs_compare}
\title{Compare two trait spaces}
\usage{
fs_compare(
  space1,
  space2,
  method = c("procrustes", "correlation"),
  dims = NULL
)
}
\arguments{
\item{space1, space2}{\code{fspace} objects with overlapping unit names.}

\item{method}{Comparison method.}

\item{dims}{Number of dimensions used from each space (default: the
smaller of the two).}
}
\value{
A list of class \code{fs_compare} with the statistic(s), the method,
and the number of shared units.
}
\description{
Quantifies the agreement between two spaces containing (at least
partially) the same units, e.g. spaces built with different methods,
different trait sets, or with/without imputed values.
}
\details{
\itemize{
\item \code{"procrustes"}: symmetric Procrustes analysis on the common units
(both configurations centred and scaled to unit sum of squares).
Reports the Procrustes statistic \code{m2} (0 = identical configurations)
and the correlation \code{sqrt(1 - m2)}.
\item \code{"correlation"}: Pearson correlation between the pairwise distance
vectors of the two spaces (a Mantel-type statistic, without the test).
}
}
\examples{
data(gspff)
s1 <- fs_space(gspff[1:200, ], method = "pca")
s2 <- fs_space(fs_dist(gspff[1:200, ]), method = "pcoa")
fs_compare(s1, s2)
fs_compare(s1, s2, method = "correlation")
}
\seealso{
\code{\link[=fs_quality]{fs_quality()}}
}
