% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_dist.R
\name{fs_dist}
\alias{fs_dist}
\title{Functional dissimilarities between units (bounded 0-1)}
\usage{
fs_dist(
  traits = NULL,
  sds = NULL,
  obs = NULL,
  ids = NULL,
  proportions = NULL,
  periods = NULL,
  cycle = 365,
  weights = NULL,
  min_obs = 4L
)
}
\arguments{
\item{traits}{Optional units x traits matrix or data.frame with unique
row names. Numeric columns are treated as quantitative traits,
factor/character/logical columns as categorical.}

\item{sds}{Optional matrix or data.frame of standard deviations for a
subset of the numeric columns of \code{traits} (same row names, column
names matching the trait names). Switches those traits to the
normal-overlap distance.}

\item{obs}{Optional data.frame of raw observations (one row per
observation, numeric columns; column names must not repeat names
already used in \code{traits}). Requires \code{ids}.}

\item{ids}{Vector (length \code{nrow(obs)}) assigning each observation to a
unit.}

\item{proportions}{Optional named list; each element is a units x
categories matrix of proportions (rows summing to 1) for one trait.}

\item{periods}{Optional named list; each element is a units x 2 matrix
(start, end) on \verb{1..cycle} for one trait; \code{start > end} wraps around
the cycle.}

\item{cycle}{Length of the cycle for \code{periods} (default 365 days).}

\item{weights}{Optional named numeric vector of trait weights (default:
equal weights). Names must match the trait names across all inputs.}

\item{min_obs}{Minimum observations per unit for the kernel route
(default 4); units below it fall back to the normal-overlap route.}
}
\value{
A \link[stats:dist]{stats::dist} object bounded in \verb{[0, 1]}, with attributes
\code{per_trait} (named list of the per-trait 0-1 \code{dist} objects),
\code{types} (named character vector of trait types) and \code{weights}.
}
\description{
Computes pairwise functional dissimilarities from multiple types of
trait information, combining them Gower-style: every trait contributes
a distance bounded between 0 and 1, and the combined dissimilarity is
their (weighted) mean, so it is itself bounded between 0 and 1. This is
the recommended input for \code{\link[=fs_space]{fs_space()}} with \code{method = "pcoa"} or
\code{"nmds"}, and the bounded scale carries through to
dissimilarity-based indices (Rao, MPD): a Rao value of 0.25 means "the
expected pair of individuals differs in a quarter of the trait range",
regardless of how many traits were used.
}
\details{
Beyond the classical Gower ingredients (quantitative traits as range-
scaled differences, categorical traits as 0/1 matching), \code{fs_dist} can
represent intraspecific variability directly in the distances,
following Carmona et al. (2015): when two units overlap in their trait
distributions, their distance is the proportion of non-overlap rather
than the difference between their means.
}
\section{Trait types}{

\itemize{
\item \strong{Quantitative, mean only} (numeric columns of \code{traits} without an
entry in \code{sds} or \code{obs}): classical Gower, \verb{|x_i - x_j| / range}.
\item \strong{Categorical} (factor/character/logical columns of \code{traits}):
0 if the two units share the level, 1 otherwise.
\item \strong{Quantitative with known spread} (numeric columns of \code{traits} with
a matching column in \code{sds}): each unit is a normal distribution
(mean from \code{traits}, sd from \code{sds}); the distance is 1 minus the
overlap of the two densities. Zero or missing sds are replaced by
the mean sd of that trait, with a warning.
\item \strong{Quantitative with raw observations} (columns of \code{obs}, grouped by
\code{ids}): each unit is a Gaussian kernel density estimate of its
observations (bandwidth \code{1.06 * sd * n^-0.2}); the distance is
1 minus the overlap of the two densities. Units with fewer than
\code{min_obs} observations fall back to a normal with the across-unit
mean sd, with a warning.
\item \strong{Category proportions} (\code{proportions}): units described by the
proportion of individuals in each level of a trait; the distance is
1 minus the summed pairwise minima of the proportions.
\item \strong{Periods} (\code{periods}): units described by a start and an end on a
cycle (e.g. flowering period in days of the year, \code{cycle = 365},
wrap-around allowed); the distance is 1 minus the shared fraction of
the shorter period.
}

All types can be mixed freely; each trait contributes one 0-1 distance
matrix and the combined dissimilarity is their weighted mean. Missing
values in the numeric/categorical columns of \code{traits} are tolerated:
that trait is simply skipped for pairs where either unit is \code{NA}, and
the mean is taken over the traits available to each pair (classical
Gower behaviour).
}

\examples{
data(gspff)
d <- fs_dist(gspff[1:80, ])          # range-scaled Gower, all numeric
range(d)                             # bounded 0-1
sp <- fs_space(d, method = "pcoa")   # the intended workflow

# intraspecific variability from raw observations:
data(grassland)
tr <- data.frame(height = log10(grassland$height),
                 sla = log10(grassland$sla))
d2 <- fs_dist(obs = tr, ids = grassland$species)
attr(d2, "types")
}
\references{
Carmona, C.P., Rota, C., Azcárate, F.M. & Peco, B. (2015)
More for less: sampling strategies of plant functional traits across
local environmental gradients. \emph{Functional Ecology}, 29, 579-588.
}
\seealso{
\code{\link[=fs_space]{fs_space()}} (use the result as its \code{traits} input for
\code{method = "pcoa"} or \code{"nmds"}).
}
