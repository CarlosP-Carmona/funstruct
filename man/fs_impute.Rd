% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_impute.R
\name{fs_impute}
\alias{fs_impute}
\title{Impute missing trait values}
\usage{
fs_impute(traits, phylo = NULL, n_eigen = 10L, seed = NULL, ...)
}
\arguments{
\item{traits}{Data.frame (units x traits) with unique row names;
numeric and factor columns are supported. Missing values (\code{NA}) are
imputed.}

\item{phylo}{Optional phylogeny (class \code{phylo}, \pkg{ape}) whose tip
labels include all row names of \code{traits}. Its cophenetic distances
are converted to eigenvectors used as additional predictors (and
dropped from the output).}

\item{n_eigen}{Number of phylogenetic eigenvectors (default 10, capped
at units - 1).}

\item{seed}{Random seed.}

\item{...}{Passed to \code{\link[missForest:missForest]{missForest::missForest()}}.}
}
\value{
The completed trait data.frame, with attribute \code{imputed}: a
list with \code{cells} (a units x traits logical matrix marking imputed
values), \code{units} (row names with at least one imputed value),
\code{complete} (row names that were fully empirical, useful to build a
space from empirical data only and project the rest with
\code{\link[=fs_project]{fs_project()}}) and \code{oob} (missForest out-of-bag error estimate).
}
\description{
Random-forest imputation of missing trait values via
\pkg{missForest} (Stekhoven & Buhlmann 2012), optionally informed by
phylogeny through phylogenetic eigenvectors (the funspace approach).
Which values were imputed is recorded in the result and propagated by
\code{\link[=fs_space]{fs_space()}} into the space's unit metadata, so imputation is never
invisible downstream.
}
\details{
This is a convenience wrapper, not an imputation research tool: for
serious gap-filling exercises, evaluate imputation quality for your
data and traits.
}
\examples{
\donttest{
data(gspff_missing)
data(gspff_phylo)
set.seed(1)
sub <- gspff_missing[sample(nrow(gspff_missing), 150), ]

out <- fs_impute(sub, seed = 1)
attr(out, "imputed")$oob            # out-of-bag error estimate

# informed by the phylogeny:
outp <- fs_impute(sub, phylo = gspff_phylo, n_eigen = 5, seed = 1)
head(attr(outp, "imputed")$units)

# recommended workflow: build the space from EMPIRICAL data only and
# project the imputed species into it (same centring and scaling):
data(gspff)
sp <- fs_reduce(fs_space(gspff, method = "pca"), 2)
proj <- fs_project(sp, out)
head(proj)
}
}
\references{
Stekhoven, D.J. & Buhlmann, P. (2012) MissForest:
non-parametric missing value imputation for mixed-type data.
\emph{Bioinformatics}, 28, 112-118.
}
\seealso{
\code{\link[=fs_space]{fs_space()}}
}
