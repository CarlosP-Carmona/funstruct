% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_extras.R
\name{fs_itv_contribution}
\alias{fs_itv_contribution}
\title{Contribution of intraspecific variability to functional structure}
\usage{
fs_itv_contribution(space, comm = NULL, indices = c("richness", "dispersion"))
}
\arguments{
\item{space}{An \code{fspace} with observation-based TPDs.}

\item{comm}{Community matrix or long data.frame; see \code{\link[=fs_structure]{fs_structure()}}.}

\item{indices}{Indices to evaluate (default \code{"richness"} and
\code{"dispersion"}, where the interpretation is cleanest).}
}
\value{
A list of class \code{fs_itv_contribution}: \code{full}, \code{mean_based}
(both \code{fstructure}) and \code{contribution} (data.frame, same shape).
}
\description{
\emph{Novel index.} Quantifies how much of each assemblage's functional
structure is contributed by within-unit (intraspecific) trait
variability. Structure is computed twice with identical bandwidths:
from the full unit TPDs (individual observations) and from TPDs
collapsed to each unit's mean position. The contribution is
\code{(full - mean_based) / full} per index and assemblage: 0 means the
index is fully explained by mean trait positions, values toward 1 mean
within-unit variability dominates. Requires TPDs estimated from
observations (\code{\link[=fs_tpd]{fs_tpd()}} with \code{ids}).
}
\examples{
set.seed(1)
tr <- data.frame(t1 = rnorm(60, rep(c(0, 3, 6), each = 20), 0.5),
                 t2 = rnorm(60, rep(c(0, 3, 0), each = 20), 0.5),
                 row.names = paste0("i", 1:60))
ids <- rep(c("a", "b", "c"), each = 20)
sp <- fs_space(tr, method = "raw", scale = FALSE)
tp <- fs_tpd(sp, ids = ids)

fs_itv_contribution(tp)
}
\seealso{
\code{\link[=fs_itv]{fs_itv()}} for the effect of ITV on the space itself.
}
