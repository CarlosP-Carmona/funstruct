% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_loadings.R
\name{fs_loadings}
\alias{fs_loadings}
\title{Post-hoc trait loadings for dissimilarity-based spaces}
\usage{
fs_loadings(space, traits, method = c("pearson", "spearman"))
}
\arguments{
\item{space}{A \code{pcoa} or \code{nmds} \code{fspace} (PCA and raw spaces already
carry construction-time loadings, or are the traits themselves).}

\item{traits}{Matrix or data.frame of the trait values the
dissimilarities were computed from, with row names covering all
units of the space. Missing values are tolerated (pairwise-complete
correlations).}

\item{method}{Correlation type for the numeric traits: \code{"pearson"}
(default) or \code{"spearman"}.}
}
\value{
The \code{fspace} with \verb{$loadings} filled: a numeric-traits x axes
matrix of correlations, with attributes \code{method} and (when
categorical traits are present) \code{categorical} (traits x axes matrix
of eta values).
}
\description{
Computes correlations between the original traits and the axes of a
PCoA or NMDS space, and stores them as the space's \code{loadings} so that
\code{plot()} can draw trait arrows. This formalizes standard
practice for dissimilarity-based ordinations (compare
\code{mFD::traits.faxes.cor()} and \code{vegan::envfit()}).
}
\details{
The convention is continuous with PCA: when the dissimilarity is the
Euclidean distance on scaled traits, PCoA reproduces the PCA
configuration and these correlations coincide (up to axis sign) with
the loadings \code{\link[=fs_space]{fs_space()}} reports for PCA.

Two caveats, both consequences of the axes being functions of
dissimilarities rather than linear combinations of traits:
\itemize{
\item These loadings are \emph{descriptive}, computed after the fact. They do
not define the axes and cannot be used to project new units into the
space (\code{\link[=fs_project]{fs_project()}} remains PCA-only).
\item A correlation only captures the linear part of a trait-axis
relationship. Gower-based axes often relate to traits monotonically
but not linearly; \code{method = "spearman"} is the more honest choice in
that case.
}

Numeric traits enter the loadings matrix as (signed) correlations.
Categorical traits have no direction, so they are not drawn as
arrows; their association with each axis is reported separately as the
square root of the correlation ratio (eta, in \verb{[0, 1]}, the
multiple-correlation analogue used by mFD) in
\code{attr(space$loadings, "categorical")}.
}
\examples{
data(gspff)
x <- gspff[1:150, ]
sp <- fs_space(fs_dist(x), method = "pcoa")
sp <- fs_loadings(sp, x)
round(sp$loadings[, 1:2], 2)
plot(sp)   # trait arrows, now also for PCoA
}
\seealso{
\code{\link[=fs_space]{fs_space()}}, \code{\link[=fs_dist]{fs_dist()}}, \code{\link[=fs_angles]{fs_angles()}}
}
