% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_means.R
\name{fs_means}
\alias{fs_means}
\title{Aggregate individual observations into unit means}
\usage{
fs_means(obs, ids)
}
\arguments{
\item{obs}{Data.frame or matrix of individual observations (rows) by
numeric traits (columns).}

\item{ids}{Vector (length \code{nrow(obs)}) assigning each observation to a
unit.}
}
\value{
A data.frame of per-unit trait means (units as row names, in
order of first appearance in \code{ids}), with attributes \code{n} (named
integer vector of observations per unit) and \code{sds} (data.frame of
per-unit, per-trait standard deviations; \code{NA} for units with a
single observation). Missing values are ignored within each unit.
}
\description{
Computes one mean trait value per unit (e.g. per species) from
individual-level observations. This is the entry point of the
mean-based workflow for unbalanced sampling: when units differ in
replication, an ordination built directly on the individuals lets the
well-sampled units pull the axes. Building the space on one mean per
unit weights every unit equally, and the individuals are then
projected into that space afterwards -- with \code{\link[=fs_project]{fs_project()}} for the
coordinates, or in one step with \code{\link[=fs_tpd]{fs_tpd()}}\verb{(space, obs = , ids = )}
for trait probability densities.
}
\examples{
data(grassland)
tr <- data.frame(height = log10(grassland$height),
                 sla = log10(grassland$sla),
                 row.names = paste0("ind", seq_len(nrow(grassland))))

trM <- fs_means(tr, grassland$species)   # one row per species
head(trM)
head(attr(trM, "n"))

# mean-based space, individuals projected afterwards:
sp <- fs_space(trM, method = "pca")
co <- fs_project(sp, tr)                 # individual coordinates
head(co)
}
\seealso{
\code{\link[=fs_space]{fs_space()}}, \code{\link[=fs_project]{fs_project()}}, \code{\link[=fs_tpd]{fs_tpd()}}
}
