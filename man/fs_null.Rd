% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_null.R
\name{fs_null}
\alias{fs_null}
\title{Null models for functional structure}
\usage{
fs_null(
  space,
  comm = NULL,
  engine = c("prob", "points"),
  indices = NULL,
  model = c("shuffle.units", "shuffle.abund"),
  n = 499L,
  seed = NULL,
  cores = 1L
)
}
\arguments{
\item{space}{An \code{fspace} (with TPDs for the probabilistic engine).}

\item{comm}{Community matrix or long data.frame; see \code{\link[=fs_structure]{fs_structure()}}.}

\item{engine, indices}{Passed to \code{\link[=fs_structure]{fs_structure()}}.}

\item{model}{Null model; see Details.}

\item{n}{Number of randomizations (default 499).}

\item{seed}{Random seed.}

\item{cores}{Number of cores (unix only; Windows runs serially).}
}
\value{
The observed \code{fstructure} with additional columns \verb{ses_*} and
\verb{p_*} for every index, and an attribute \code{nulls} with the null means
and standard deviations.
}
\description{
Computes functional structure (\code{\link[=fs_structure]{fs_structure()}}) together with null
expectations, standardized effect sizes (SES) and two-sided p-values.
Unit TPDs and bandwidths are estimated once and never re-selected
inside the randomizations: null models permute identities or
abundances, not the smoothing.
}
\details{
Null models:
\itemize{
\item \code{"shuffle.units"}: unit identities are permuted globally (the
classical taxa-labels null): each assemblage keeps its abundance
structure, but which trait profile carries each abundance is
randomized consistently across assemblages.
\item \code{"shuffle.abund"}: abundances are permuted independently within each
assemblage across all units.
}
}
\examples{
sp <- fs_space(data.frame(size = c(0, 2, 4), shape = c(0, 1, 0),
                          row.names = c("sp1", "sp2", "sp3")),
               method = "raw", scale = FALSE)
tp <- fs_tpd(sp, sds = 0.4)
comm <- rbind(A = c(sp1 = 0.7, sp2 = 0.3, sp3 = 0),
              B = c(sp1 = 0.2, sp2 = 0.3, sp3 = 0.5))

st <- fs_null(tp, comm, indices = c("richness", "dispersion"),
              n = 49, seed = 1)
st[, c("richness", "ses_richness", "p_richness")]
}
\seealso{
\code{\link[=fs_structure]{fs_structure()}}
}
