% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_partition.R
\name{fs_partition}
\alias{fs_partition}
\title{Partition functional diversity across nested scales}
\usage{
fs_partition(
  space,
  comm,
  hierarchy = NULL,
  method = c("rao", "hill", "tpd_eqv"),
  q = 1,
  profile = FALSE
)
}
\arguments{
\item{space}{An \code{fspace}; \code{"tpd_eqv"} (and \code{"rao"} with overlap
dissimilarities) require TPDs (see \code{\link[=fs_tpd]{fs_tpd()}}).}

\item{comm}{Community matrix (assemblages x units) or long data.frame;
see \code{\link[=fs_structure]{fs_structure()}}.}

\item{hierarchy}{An \code{\link[=fs_hierarchy]{fs_hierarchy()}} with one row per assemblage (rows
in the order of \code{comm}), or \code{NULL} for a two-level partition
(assemblages vs the whole dataset).}

\item{method}{Partition method; see Details.}

\item{q}{Dominance parameter for \code{"tpd_eqv"} (default 1; \code{q = 0}
ignores abundances).}

\item{profile}{Logical; for \code{"tpd_eqv"}, also compute the full profile
over \code{q = seq(0, 2, 0.1)}.}
}
\value{
A list of class \code{fpartition}: \code{method}, \code{levels}, \code{values}
(per-assemblage and per-group values, and gamma), \code{table} (the
additive components), and \code{profiles} (when requested).
}
\description{
Decomposes diversity additively across any number of nested levels
(assemblages, then the levels declared with \code{\link[=fs_hierarchy]{fs_hierarchy()}}, up to the
whole dataset).
}
\details{
\itemize{
\item \code{method = "rao"}: Rao quadratic entropy with the additive partition
and the equivalent-number correction of de Bello et al. (2010).
Dissimilarities between units are overlap-based (1 minus shared TPD
mass) when TPDs are present, and max-scaled Euclidean distances in
the space otherwise. \code{q} is not used by this method.
\item \code{method = "tpd_eqv"}: TPD-based equivalent numbers (Castro
Sanchez-Bermejo et al.): each unit's TPD is rescaled by
\code{(p/p_max)^q} (relative abundances within the assemblage) and the
equivalent number is the integral of the pointwise maximum across
units. Aggregation to higher levels takes the pointwise maximum of
the lower-level maximum functions, without re-weighting, giving a
non-negative additive partition at every level.
\item \code{method = "hill"}: not yet implemented (planned; Hill-Chao framework).
}
}
\examples{
sp <- fs_space(data.frame(size = c(0, 2, 4), shape = c(0, 1, 0),
                          row.names = c("sp1", "sp2", "sp3")),
               method = "raw", scale = FALSE)
tp <- fs_tpd(sp, sds = 0.4)
comm <- rbind(A1 = c(sp1 = 1, sp2 = 0, sp3 = 0),
              A2 = c(sp1 = 0, sp2 = 1, sp3 = 0),
              A3 = c(sp1 = 0, sp2 = 0, sp3 = 1),
              A4 = c(sp1 = 1, sp2 = 1, sp3 = 1))
h <- fs_hierarchy(site = c("s1", "s1", "s2", "s2"))

fs_partition(tp, comm, hierarchy = h, method = "tpd_eqv", q = 0)
fs_partition(tp, comm, hierarchy = h, method = "rao")

# full q-profile of the equivalent numbers
pt <- fs_partition(tp, comm, method = "tpd_eqv", profile = TRUE)
plot(pt)
}
\references{
de Bello, F. et al. (2010) The partitioning of diversity:
showing Theseus a way out of the labyrinth. \emph{Journal of Vegetation
Science}, 21, 992-1000.
}
\seealso{
\code{\link[=fs_hierarchy]{fs_hierarchy()}}, \code{\link[=fs_structure]{fs_structure()}}, \code{\link[=fs_tpd]{fs_tpd()}}
}
