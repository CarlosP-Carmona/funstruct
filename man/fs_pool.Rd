% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_pool.R
\name{fs_pool}
\alias{fs_pool}
\title{One-step pooled assemblage TPDs}
\usage{
fs_pool(space, ids, comm, bw = NULL, grid = NULL, alpha = 0.99)
}
\arguments{
\item{space}{An \code{fspace} whose rows are individual observations.}

\item{ids}{Vector assigning each row to a unit.}

\item{comm}{Community matrix (assemblages x units) or long data.frame.}

\item{bw}{Optional bandwidth (numeric per-axis SDs or a d x d matrix);
default: one plug-in selection on all observations pooled.}

\item{grid}{An \code{fs_grid}; default \code{fs_grid(space)}.}

\item{alpha}{Probability mass retained (default 0.99).}
}
\value{
The \code{fspace} with \code{tpds} whose units are the \emph{assemblages}
(one pooled TPD each); downstream functions such as \code{\link[=fs_beta]{fs_beta()}} can
then compare assemblages directly (use an identity community
matrix).
}
\description{
Builds one TPD per assemblage directly from the pooled individual
observations of its constituent units, ignoring unit identities and
abundances (every individual contributes equally).
}
\details{
\strong{This is usually the wrong tool.} Pooling observations without
accounting for unit identity introduces pseudoreplication (individuals
of the same species cluster in trait space), makes results depend on
how many units happen to be sampled, and, if bandwidths were selected
per pooled sample, lets the smoothing covary with sample composition
(Tordoni et al.; see also Gross et al. 2024 and its reanalysis). The
default workflow (\code{\link[=fs_tpd]{fs_tpd()}} then \code{\link[=fs_structure]{fs_structure()}}) avoids all three
problems. \code{fs_pool()} exists for comparisons and for reproducing
pooled-observation analyses; it uses one single bandwidth selected
once from all observations, so at least the smoothing cannot covary
with composition.
}
\examples{
set.seed(1)
tr <- data.frame(t1 = rnorm(60, rep(c(0, 3, 6), each = 20), 0.5),
                 t2 = rnorm(60, rep(c(0, 3, 0), each = 20), 0.5),
                 row.names = paste0("i", 1:60))
ids <- rep(c("a", "b", "c"), each = 20)
sp <- fs_space(tr, method = "raw", scale = FALSE)
comm <- rbind(P = c(a = 1, b = 1, c = 0),
              Q = c(a = 0, b = 1, c = 1))

pooled <- fs_pool(sp, ids, comm)   # note the warning: this is the
names(pooled$tpds$units)           # approach the package discourages
}
\seealso{
\code{\link[=fs_tpd]{fs_tpd()}} for the recommended two-step construction.
}
