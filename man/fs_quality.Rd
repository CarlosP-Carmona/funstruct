% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_quality.R
\name{fs_quality}
\alias{fs_quality}
\title{Quality of a trait space across dimensionalities}
\usage{
fs_quality(space, dims = NULL)
}
\arguments{
\item{space}{An \code{fspace} object. The initial dissimilarities are taken
from the distance matrix stored in the space (PCoA/NMDS) or computed
from the stored traits (PCA/raw).}

\item{dims}{Candidate dimensionalities to evaluate. Default: \code{1} up to
\verb{min(10, available axes)}.}
}
\value{
A data.frame of class \code{fs_quality} with columns \code{dims}, \code{mAD}
and \code{mSD}, and an attribute \code{best} (dimensionality with lowest mSD).
}
\description{
Measures how faithfully the first \code{k} axes of a space preserve the
original functional dissimilarities between units, following the logic of
Maire et al. (2015). For each candidate dimensionality, the Euclidean
distances in the reduced space are compared with the initial
dissimilarities (both scaled to unit maximum); the mean absolute
deviation (mAD) and mean squared deviation (mSD) summarize the mismatch
(0 = perfect preservation).
}
\examples{
data(gspff)
sp <- fs_space(gspff[1:300, ], method = "pca")
fs_quality(sp)
}
\references{
Maire, E., Grenouillet, G., Brosse, S. & Villeger, S. (2015)
How many dimensions are needed to accurately assess functional
diversity? A pragmatic approach for assessing the quality of
functional spaces. \emph{Global Ecology and Biogeography}, 24, 728-740.
}
\seealso{
\code{\link[=fs_dimensionality]{fs_dimensionality()}}, \code{\link[=fs_reduce]{fs_reduce()}}
}
