% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_extras.R
\name{fs_redundancy}
\alias{fs_redundancy}
\title{Continuous functional redundancy profiles}
\usage{
fs_redundancy(space, comm = NULL, q = seq(0, 2, 0.1))
}
\arguments{
\item{space}{An \code{fspace} with TPDs (\code{\link[=fs_tpd]{fs_tpd()}}).}

\item{comm}{Community matrix or long data.frame; see \code{\link[=fs_structure]{fs_structure()}}.}

\item{q}{Numeric vector of dominance parameters (default
\code{seq(0, 2, 0.1)}).}
}
\value{
A list of class \code{fs_redundancy}: \code{values} (long data.frame:
assemblage, q, redundancy). \code{print()} and \code{plot()} methods provided.
}
\description{
\emph{Novel index.} Extends the TPD redundancy of \code{\link[=fs_structure]{fs_structure()}}
(abundance-weighted mean number of units co-occupying a cell, minus 1)
into a profile over the dominance parameter \code{q}: abundances are raised
to \code{q} and renormalized before mixing, so \code{q = 0} weighs all present
units equally and larger \code{q} concentrates the assemblage TPD on
dominant units. The profile shows whether redundancy is carried by the
whole assemblage or only by dominant units, linking redundancy to
functional insurance.
}
\examples{
sp <- fs_space(data.frame(size = c(0, 0.3, 4), shape = c(0, 0.2, 0),
                          row.names = c("sp1", "sp2", "sp3")),
               method = "raw", scale = FALSE)
tp <- fs_tpd(sp, sds = 0.4)
comm <- rbind(A = c(sp1 = 0.6, sp2 = 0.3, sp3 = 0.1))

red <- fs_redundancy(tp, comm, q = c(0, 1, 2))
red
plot(red)
}
\seealso{
\code{\link[=fs_structure]{fs_structure()}}
}
