% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_sensitivity.R
\name{fs_sensitivity}
\alias{fs_sensitivity}
\title{Bandwidth sensitivity of functional structure}
\usage{
fs_sensitivity(
  space,
  comm = NULL,
  range = c(0.5, 1.5),
  steps = 5L,
  indices = NULL
)
}
\arguments{
\item{space}{An \code{fspace} with TPDs estimated by \code{\link[=fs_tpd]{fs_tpd()}} (which
stores the observations needed to re-evaluate the densities).}

\item{comm}{Community matrix or long data.frame; see \code{\link[=fs_structure]{fs_structure()}}.}

\item{range}{Multiplicative range for the kernel standard deviations
(default \code{c(0.5, 1.5)}).}

\item{steps}{Number of factors evaluated across \code{range} (default 5).}

\item{indices}{Passed to \code{\link[=fs_structure]{fs_structure()}}.}
}
\value{
A list of class \code{fs_sensitivity}: \code{values} (long data.frame:
factor, assemblage, one column per index) and \code{stability} (per
index: correlation of assemblage values between the two extreme
factors, and the mean relative range across factors). \code{print()} and
\code{plot()} methods are provided.
}
\description{
Recomputes functional structure with all unit bandwidths scaled by a
range of factors (default 0.5 to 1.5, i.e. +/- 50\%), packaging the
sensitivity protocol of Tordoni et al.: if conclusions survive halving
and inflating the smoothing, they do not hinge on the bandwidth.
Bandwidths are scaled, never re-selected.
}
\examples{
sp <- fs_space(data.frame(size = c(0, 2, 4), shape = c(0, 1, 0),
                          row.names = c("sp1", "sp2", "sp3")),
               method = "raw", scale = FALSE)
tp <- fs_tpd(sp, sds = 0.4)

sen <- fs_sensitivity(tp, steps = 3, indices = "richness")
sen
plot(sen, index = "richness")
}
\seealso{
\code{\link[=fs_tpd]{fs_tpd()}}, \code{\link[=fs_structure]{fs_structure()}}
}
