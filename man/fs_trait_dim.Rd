% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_trait_dim.R
\name{fs_trait_dim}
\alias{fs_trait_dim}
\title{How much does each trait add to the dimensionality of the space?}
\usage{
fs_trait_dim(
  traits,
  trait = NULL,
  q = 2,
  n_null = 199L,
  scale = TRUE,
  seed = NULL
)
}
\arguments{
\item{traits}{Units x traits matrix or data.frame (all numeric, no
missing values -- impute first, see \code{\link[=fs_impute]{fs_impute()}}), or a PCA
\code{fspace} whose stored traits are used.}

\item{trait}{Optional name of a single target trait; default \code{NULL}
analyzes every trait in turn.}

\item{q}{Hill order for the END (default 2; see
\code{\link[=fs_dimensionality]{fs_dimensionality()}}).}

\item{n_null}{Number of permutations of the target trait (default
199).}

\item{scale}{Logical; compute the PCA on correlations (default
\code{TRUE}) or covariances.}

\item{seed}{Random seed for the permutations.}
}
\value{
A list of class \code{fs_trait_dim} with \code{end_full} (END of the
complete space), \code{q}, \code{n_null}, and \code{table}: one row per target
trait with \code{end_without} (END of the space without it), \code{delta}
(observed contribution), \code{null_mean}, \code{null_lo}, \code{null_hi} (mean
and 2.5/97.5 percentiles of the permutation contributions),
\code{ratio} (\code{delta / null_mean}; 0 = fully redundant, 1 = adds as much
as a random independent trait) and \code{p_random} (proportion of null
contributions <= the observed one; small values mean the trait adds
significantly less than a random trait). \code{print()} and \code{plot()}
methods are provided.
}
\description{
Quantifies the information a trait contributes to a PCA trait space
through its effect on the effective number of dimensions (END; the
Hill number of order \code{q} of the eigenvalue shares -- with the default
\code{q = 2}, the inverse Simpson index; Beccari & Carmona 2024). For a
target trait, the observed contribution is
}
\details{
\deqn{\Delta = END(all traits) - END(all traits except the target)}

and its yardstick is a null model that permutes the target trait's
values across units: a permuted trait is uncorrelated with the rest,
so it adds close to one full effective dimension. Comparing the
observed \eqn{\Delta} with the null distribution tells you where the
trait sits between two poles:
\itemize{
\item \eqn{\Delta} close to the null (ratio near 1, \code{p_random} not
small): the trait behaves like an independent axis of variation --
the other traits do not capture it, and dropping it would discard
information.
\item \eqn{\Delta} far below the null (ratio near 0, small \code{p_random}):
the trait is largely redundant with the existing set -- the space
without it is nearly the same space.
}

With \code{trait = NULL} the analysis runs for every trait, which screens
the whole battery at once. Used iteratively, this supports backward
selection of the simplest sufficient space: drop the trait with the
smallest contribution, rebuild, and repeat while the smallest
contribution stays below your retention criterion. The stopping
threshold is a scientific choice, not a statistical one, so the
package deliberately does not automate it. Note that contributions
are conditional on the current trait set: after dropping a trait, the
remaining contributions change, so re-run at each step rather than
dropping several traits in one pass.
}
\examples{
data(gspff)
x <- gspff[1:300, ]
td <- fs_trait_dim(x, n_null = 99, seed = 1)
td
plot(td)

# a single target trait:
fs_trait_dim(x, trait = "ph", n_null = 99, seed = 1)
}
\references{
Beccari, E. & Carmona, C.P. (2024) Aboveground and
belowground sizes are aligned in the unified spectrum of plant form
and function. \emph{Nature Communications}, 15.
\doi{10.1038/s41467-024-53180-x}
}
\seealso{
\code{\link[=fs_dimensionality]{fs_dimensionality()}} (\code{method = "end"}), \code{\link[=fs_space]{fs_space()}}
}
